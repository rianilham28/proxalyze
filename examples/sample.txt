# proxalyze sample input — declare the protocol on the command line:
#   proxalyze -i examples/sample.txt:http
# Recognised line forms (protocol never guessed from a bare address; these
# use x.x.x.x placeholders so they are NOT parsed as proxies):
#   x.x.x.x:8080
#   socks5://x.x.x.x:1080
#   http://user:pass@x.x.x.x:8080
#   user:pass@x.x.x.x:8080
#   x.x.x.0/24:8080
# Real (documentation-range, unroutable) candidates below:
203.0.113.10:8080
http://bob:s3cret@203.0.113.11:3128
socks5://203.0.113.12:1080
